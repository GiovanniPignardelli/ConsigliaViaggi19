String[] parsed;

"ANTICA SBOBBO NAPOLI PIZZERIA LUPE23N"

topQuery = null;
query=null;

for(int i=0; parsed.lenght(); i++){
	if(query==null)
	{
		query=topQuery;
	}
	query = query.where(searchTags,"array-contains",parsed[i]).limit(200);
	if(query != null){
		weakQuery = topQuery;
		topQuery = query;	
	}
}

